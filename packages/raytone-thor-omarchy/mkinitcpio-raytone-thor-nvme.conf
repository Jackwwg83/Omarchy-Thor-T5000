# RaytoneOS Jetson AGX Thor: the NVMe install's root is behind the PCIe controller and NVMe drivers,
# which NVIDIA's kernel builds as modules; JetPack's own initrd carries the same three.
MODULES+=(pcie_tegra264 phy_tegra194_p2u nvme)
