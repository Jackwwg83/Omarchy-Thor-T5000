| 原发现 | 状态 | 原因 |
|---|---|---|
| 1. 温控失败处理 | 部分 | 已检查风扇服务及 95°C 阈值，但仅执行一次，传感器全部读取失败仍按 0°C 放行。 |
| 2. chroot 隔离不完整 | 部分 | 已隔离 PID、挂载及设备并削减权限，但 `/proc/sys` 等共享内核接口仍未只读或屏蔽。 |
| 3. dead-man / watchdog 缺口 | 部分 | 定时器已提前至 sysinit，但仍依赖正常重启路径，未验证硬件 watchdog 实际生效。 |
| 4. `.sig` 通配误选 | 已解决 | 包文件筛选明确排除 `.sig`。 |
| 5. 缺少安装后验证 | 部分 | 已检查关键产物和配置，但批量 `is-enabled` 只需一个单元启用即可成功，不能证明全部启用（[官方说明](https://github.com/systemd/systemd/blob/main/man/systemctl.xml)）。 |
| 6. NetworkManager 配置来源 | 已解决 | 改为从宿主机当前活动连接的实际文件路径复制，并要求至少找到一个配置。 |
| 7. 账户 / SSH 预检太晚 | 已解决 | 密码哈希和非空 `authorized_keys` 检查均提前到写入之前。 |
| 8. xtrace / sudo 处理 | 已解决 | 读取密码前关闭追踪，哈希通过标准输入传递；sudo 文件重建后设为 0440 并检查语法。 |
| 9. 部分解包与清理 | 部分 | 完成标记及非空目录检查已补齐；清理仍忽略卸载失败，也未显式清理 tools 下的两个绑定挂载。 |
| 10. 早期启动标记 / locale / localtime | 已解决 | 增加 remount 后的早期标记、运行 `locale-gen`，并按宿主 localtime 类型复制或建立链接。 |

新增阻断问题：未发现。本结论为静态复核；上述残余项不阻断此次写盘，未据此认定无人值守启动已可靠。

可以执行 (写入 U 盘)
